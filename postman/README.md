# Postman setup

Import `nse-tracker.postman_collection.json` directly.

The environment file (`baseUrl`, `readToken`, `adminToken`) isn't
committed — it holds real credentials once filled in. Create your own
in Postman: Environments → Create Environment, add those three
variables, select it before running requests.
